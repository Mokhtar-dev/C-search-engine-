#include <stdio.h>  //standard input/output library, it contains functions for input and output operations (like printf and scanf)
int main() {
    // this is my first C program:

    /*this is
    a multi-line comment*/

    printf("Hello, World!\n");

    printf("This is a simple C program.\n");  // the  \n is used to print a new line
    //=========================================================================================================================================================================================

    return 0;  // the return statement is used to exit the main function and return a value to the operating system, it is optional but it is a good practice to include it to indicate that the program has ended successfully
}